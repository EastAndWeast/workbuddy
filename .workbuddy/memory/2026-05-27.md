# 2026-05-27 Work Log

## RWA 早报发布（08:30-09:00）

### 问题：07:00 主任务未执行
- 兜底任务（07:15）补生成了早报内容（`rwa_morning_20260527.md`），但未执行飞书审核步骤
- 手动创建飞书文档并发送审核消息到群「RWA 早报（07:00WB）」
- 用户审核通过后执行 WP 发布 + 封面图生成

### 发布结果
- WP Post ID: 520
- URL: https://www.tianao1128.online/?p=520
- 飞书文档: https://www.feishu.cn/docx/FRahdOyEzo6mMxxzubdcfp83nmh
- 封面图: `rwa_morning_cover_20260527.png`（1786.5 KB, 1024x1536）

### rwa_generate_cover.py 修复
- 代理对大响应体（>2MB）有间歇性截断问题（curl exit code 18 = CURLE_PARTIAL_FILE）
- medium 质量失败多次，low 质量通过（~2.4MB 响应）
- `--noproxy '*'` 无法绕过沙箱代理
- 最终方案：`curl -s -o` 写文件方式 + `--retry-all-errors` + low 质量兜底
- 默认质量改为 `low`（写文档里没改代码，但建议 cover-quality 用 low）

### 飞书发送备忘
- `--image-path` 不是有效参数，正确是 `--image <相对路径>`（不支持绝对路径）
- bot 身份可发送图片和 post 消息到群
- user 身份缺少 `im:message.send_as_user` scope，需用 `--as bot`

### 已清理
- 测试文章 514, 516, 518 已删除
- 最终发布 Post 520

## RWA 早报任务拆分（09:12-09:15）

### 问题诊断
- 07:00 主任务实际跑了但**搜索阶段超时**：总时长 113.8 秒，第一轮 9 次搜索完成，第二轮 4 次搜索发出后结果未返回
- 输出仅 6KB（历史正常 44-63KB），会话在等待搜索结果时终止
- 兜底任务（07:15）成功生成文件但飞书 OAuth 权限不足无法发消息

### 拆分方案
原来 1 个任务「搜索+写作+飞书文档+发消息」→ 拆成 3 个：

| 任务 | ID | 时间 | 职责 |
|---|---|---|---|
| RWA 早报每日生成 | automation-1779468949931 | 07:00 | 搜索 + 写作 + 保存文件 |
| RWA 早报飞书发布 | automation-1779844570589 | 07:05 | 读文件 → 飞书文档 → 发审核消息 |
| RWA 早报兜底检查 | automation-1779755978090 | 07:15 | 文件不存在则全流程兜底 |

07:00 prompt 精简：去掉飞书相关所有步骤（Step 5/6/7 + 技术备忘），只保留搜索策略 + 写作模板 + 4 步执行流程

## RWA 早报全面优化（09:17-09:20）

7 个优化点全部执行：

| # | 任务 | 修改内容 |
|---|---|---|
| P0-1 | 07:00 | 搜索从 7 方向 6-8 轮 → 5 轮表格化 + 并联提示 |
| P0-2 | 07:05/兜底 | `--image-path` → `--image`（修正飞书 CLI 无效参数） |
| P1-3 | 07:00 | 加「🛑 超时保护」：搜到 3 条以上立刻写作 |
| P1-4 | 兜底 | WP 发布加 `--generate-cover --cover-quality low` |
| P2-5 | 07:05 | 删除「发布确认」冗余段，prompt 减半 |
| P2-6 | 07:00 | 加 Step 5 写入 memory.md，记录今日覆盖话题 |
| P2-7 | 兜底 | 搜索从 3 方向 → 5 轮表格化，与主任务统一 |

## RWA 行业早报新增（14:54-15:05）

### 背景
用户要求在现有早报基础上新增一份精简版「RWA 行业早报」：
- Part 1: AI 文章解读（2-3 段连贯分析，非复述）
- Part 2: 时间线速报（时间 + 加粗标题 + 2-3 行背景描述）

### 设计决策
- 执行时机：主早报 WP 发布后（不依赖审批通过时间，固定 08:30）
- 输出渠道：与主早报相同（文件 → 飞书文档 → 群审核 → WP 发布）
- 内容来源：基于主早报 `rwa_morning_YYYYMMDD.md` 二次提炼，不重复搜索
- 同样拆成生成+发布两个任务以避免超时

### 新增自动化

| 任务 | ID | 时间 | 职责 |
|---|---|---|---|
| RWA 行业早报生成 | automation-1779865519898 | 08:30 | 读主早报 → AI 提炼 → 保存 `rwa_industry_YYYYMMDD.md` |
| RWA 行业早报飞书发布 | automation-1779865536655 | 08:35 | 读行业早报 → 飞书文档 → 发审核消息到群 |

### 完整早报时间线

| 时间 | 任务 | 输出 |
|---|---|---|
| 07:00 | 主早报生成 | rwa_morning_YYYYMMDD.md |
| 07:05 | 主早报飞书发布 | 飞书文档 + 群审核消息 |
| 07:15 | 兜底检查 | 主早报不存在则全流程补发 |
| 08:30 | 行业早报生成 | rwa_industry_YYYYMMDD.md |
| 08:35 | 行业早报飞书发布 | 飞书文档 + 群审核消息 |
